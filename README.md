# To-Calculate-the-Temperature.
The program calculates the temperature in degree celsius,fahrenheit and kelvin. The program is created using nested loop concept. The menu is displayed that what user want to calculate. The user takes the input and displays the parameter as selected.
